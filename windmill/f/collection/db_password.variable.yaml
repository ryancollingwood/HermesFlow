value: REPLACE_WITH_WINDMILL_COLLECTION_DB_PASSWORD
is_secret: true
description: Windmill's role password into collection_db's `collection` schema (matches WINDMILL_COLLECTION_DB_PASSWORD in the stack .env)
