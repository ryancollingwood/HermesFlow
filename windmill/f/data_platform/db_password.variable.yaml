value: REPLACE_WITH_DATA_PLATFORM_DB_PASSWORD
is_secret: true
description: data_platform role's password into collection_db's `data_platform` schema (matches DATA_PLATFORM_DB_PASSWORD in the stack .env)
