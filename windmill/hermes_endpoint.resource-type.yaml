schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  order:
    - base_url
    - api_key
  properties:
    base_url:
      type: string
      description: Base URL of the OpenAI-compatible endpoint
      default: http://hermes:8642/v1
    api_key:
      type: string
      description: Hermes gateway API_SERVER_KEY
  required:
    - base_url
    - api_key
description: Local Hermes gateway (OpenAI-compatible API)
