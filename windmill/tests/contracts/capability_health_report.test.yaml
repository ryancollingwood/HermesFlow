schema_version: "1.0"
tests:
  - id: contracts/capability-health-report
    capability_paths: [f/hermes_flow/testing/health_report]
    type: contract
    mode: promotion_gating
    script_path: f/hermes_flow/testing/health_report
    timeout_seconds: 60
    max_data_bytes: 5000000
    skip_reason: Healthy, stale-warning, failed, untested, active-version, scheduled-dependency, Windmill-link, atomic-metrics, dashboard, and schema behavior are validated directly by test_capability_health_report.py.
