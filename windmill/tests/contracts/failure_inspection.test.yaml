schema_version: "1.0"
tests:
  - id: contracts/failure-inspection-context
    capability_paths: [f/hermes_flow/repair/inspection]
    type: contract
    mode: promotion_gating
    script_path: f/hermes_flow/repair/inspection
    timeout_seconds: 60
    max_data_bytes: 131072
    skip_reason: RepairContext schema, bounds, classification, redaction, and Windmill adapter behavior are validated directly by test_failure_inspection.py.
