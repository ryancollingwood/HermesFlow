schema_version: "1.0"
tests:
  - id: contracts/product-normalization-result
    capability_paths: [f/capabilities/collection/normalise_products]
    type: contract
    mode: promotion_gating
    script_path: f/capabilities/collection/normalise_products
    timeout_seconds: 60
    max_data_bytes: 2097152
    skip_reason: Output schema contract is validated directly by test_product_normalisation.py.
