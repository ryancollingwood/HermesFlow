schema_version: "1.0"
tests:
  - id: contracts/repair-generation-result
    capability_paths: [f/hermes_flow/repair/generate_candidate]
    type: contract
    mode: promotion_gating
    script_path: f/hermes_flow/repair/generate_candidate
    timeout_seconds: 300
    max_data_bytes: 500000
    skip_reason: Repair generation, artifact retention, source-selector integration, stale-base checks, and invalid/unsafe rejection are validated with the real HF-019 wrapper and fake external clients by test_repair_candidate_generation.py.
