schema_version: "1.0"
tests:
  - id: contracts/structured-markup-result
    capability_paths: [f/capabilities/collection/extract_structured_markup]
    type: contract
    mode: promotion_gating
    script_path: f/capabilities/collection/extract_structured_markup
    timeout_seconds: 30
    max_data_bytes: 1048576
    skip_reason: Output schema contract is validated directly by test_structured_markup.py.
