# Fixture tests

Deterministic, bounded test data and fixture-driven checks. Manifest files use
`*.test.yaml`; fixture payloads must stay synthetic, secret-free, and below the
manifest's `max_data_bytes`.
