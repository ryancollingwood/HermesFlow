schema_version: "1.0"
tests:
  - id: fixture/deliberate-failure-example
    capability_paths: [f/hermes/client]
    type: fixture
    mode: promotion_gating
    script_path: f/hermes_flow/testing/example_test
    args: {outcome: fail}
    timeout_seconds: 10
    max_data_bytes: 4096
