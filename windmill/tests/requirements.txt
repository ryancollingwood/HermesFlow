# Test-only dependencies for windmill/tests/ — not part of any Windmill script's
# own lock file (those are generated per-script by `wmill generate-metadata`;
# see f/libraries/lineage/models.script.lock). pydantic/pyyaml are pinned to
# match f/hermes_flow/catalogue/models.script.lock so tests validate against
# the exact versions scripts run under.
pydantic==2.13.4
pytest==9.1.1
pyyaml==6.0.3
