# Workflow smoke and live-integration tests

End-to-end workflow cases. Use `mode: promotion_gating` for promotion evidence
and `mode: scheduled` for health checks; live tests must declare strict timeout
and data limits.
